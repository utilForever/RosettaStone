steps:
- checkout: self
  fetchDepth: 2
  submodules: true
- script: |
    sudo apt-get update -qq
    sudo apt-get install -y --no-install-recommends lcov
    mkdir build
    cd build
    cmake ..
    make -j 8
    bin/UnitTests